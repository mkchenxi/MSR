% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_folds.R
\name{split_folds}
\alias{split_folds}
\title{Randomly Split Data into K Folds for Cross-Validation}
\usage{
split_folds(data, K = 2, seed = 123)
}
\arguments{
\item{data}{A data frame, matrix, or vector to be split.}

\item{K}{Integer. Number of folds. Default is \code{2}.}

\item{seed}{Integer. Random seed for reproducibility. Default is \code{123}.}
}
\value{
A list of length \code{K}, where each element contains the subset
of the data corresponding to that fold.
}
\description{
This function randomly splits the input data into \code{K} folds for
cross-validation. Each observation is assigned to exactly one fold.
}
\examples{
# Example with a numeric vector
folds <- split_folds(1:10, K = 3)
str(folds)

# Example with a data frame
folds_df <- split_folds(iris, K = 5)
lapply(folds_df, nrow)

}
